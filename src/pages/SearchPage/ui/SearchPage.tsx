import { Search, SearchBar } from "src/features/Search";

const SearchPage = () => {
  return (
    <div>
      <SearchBar />
      <Search />
    </div>
  );
};

export default SearchPage;
